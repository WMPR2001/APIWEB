[build]
  command = "npm run build"
  functions = "functions"
  publish = "public"
  command = "npm ci && npm run build"

[build.environment]
  NODE_VERSION = "14"  # O la versión que estés usando

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
